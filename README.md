# HSK 4 Reading Practice — Lessons 1-10

This version is generated from the uploaded `Pasted markdown.md`.

It includes all 10 lessons and preserves each `课文 01-1` through `10-5` section and each source line.

## Important

The uploaded source contains Chinese text and English lesson titles, but it does **not** contain line-by-line pinyin or English translations.

Therefore `src/data/texts.js` leaves `pinyin` and `translation` empty instead of inventing them.

The React UI is already ready to display them when verified values are added.

## Run

```bash
npm install
npm run dev
```
# chinese-reading
